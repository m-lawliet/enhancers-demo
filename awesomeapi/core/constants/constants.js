const ERROR_CODES = Object.freeze({
  DEFAULT: '10000',
  NOT_FOUND: '100404',
});

const DEFAULT_RESPONSES = Object.freeze({
  OK: 'Ok',
});

module.exports = {
  ERROR_CODES,
  DEFAULT_RESPONSES,
};
